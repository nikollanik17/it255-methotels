export class HotelRoom {
  number: number;
  size: number;
  price: number;

  constructor(number: number, size: number, price: number) {
    this.number = number;
    this.size = size;
    this.price = price;
  }
}
